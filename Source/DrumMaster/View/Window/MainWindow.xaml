<Window 
    x:Class="Restless.App.DrumMaster.View.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vw="clr-namespace:Restless.App.DrumMaster.View"
    Title="{Binding DisplayName}" Height="350" Width="525" Topmost="{Binding IsTopMost}">
    <Window.Resources>
        <!-- All other resources come via App.xaml -->
        <ResourceDictionary Source="../../Resources/Dictionary/PageTemplate.xaml"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <vw:MainNavigation Grid.Row="0"  Margin="2"/>
        <Separator Grid.Row="1"/>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Auto" Padding="5">
            <ItemsControl Grid.Row="2" ItemsSource="{Binding Pages}" Margin="5"/>
        </ScrollViewer>
    </Grid>
</Window>
