<Window 
    x:Class="Restless.App.DrumMaster.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vw="clr-namespace:Restless.App.DrumMaster.View"
    Title="{Binding DisplayName}" Height="350" Width="525">
    <Window.Resources>
        <!-- All other resources come via App.xaml -->
        <ResourceDictionary Source="../../Resources/Dictionary/PageTemplate.xaml"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <vw:MainNavigation Grid.Row="0"  Margin="2"/>
        <Separator Grid.Row="1"/>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Padding="5">
            <ItemsControl ItemsSource="{Binding Pages}"/>
        </ScrollViewer>
    </Grid>
</Window>
